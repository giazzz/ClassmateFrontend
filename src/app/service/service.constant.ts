import {InjectionToken} from '@angular/core';

export const AUTHENTICATION_SERVICE_PROVIDER = new InjectionToken<string>('handle access');

export const AUTHORIZATION_SERVICE_PROVIDER = new InjectionToken<string>('handle access');
